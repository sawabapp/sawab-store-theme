# متجر صواب — ثيم سلة

ثيم Salla (Twilight) مبني على `theme-raed`. المتجر عربي/RTL يبيع حلول المياه
المنزلية (خزانات، فلاتر، مضخات، غطاسات، سخانات).

- المستودع: `sawabapp/sawab-store-theme` (خاص، تحت منظمة `sawabapp`)
- `theme_id: 121610538` · `store_id: 1474491909` (Sawab Store)

## الأوامر

```bash
pnpm run development   # بناء (يجب تشغيله بعد أي تعديل على scss/js)
pnpm run watch         # بناء مستمر
salla theme preview    # معاينة حيّة (تفاعلي — يحتاج طرفية حقيقية)
```

**مهم:** `public/*` ملفات مبنية **ومتتبَّعة في git**. أي تعديل على
`src/assets/**` يجب أن يتبعه `pnpm run development` ثم رفع الملفات المبنية معه،
وإلا لن يظهر التغيير في المتجر.

## المعاينة — قواعد تعلّمناها بصعوبة

1. `salla theme preview` يطبع رابط `s.salla.sa/design/draft-…` وهو **معاينة فقط
   بلا لوحة تحرير**. ولوحة الشركاء تفتح رابط `themes/editor/draft-…` **بلا
   `assets_url` فتظهر الصفحة بلا أي تنسيق**.
2. الرابط الصحيح يجمع الاثنين — ولّده هكذا أثناء عمل `preview`:
   ```bash
   python3 -c "import json;d=json.load(open('node_modules/.salla-cli'));print(f'https://s.salla.sa/themes/editor/draft-{d[\"draft_id\"]}?assets_url=http://localhost:8000&legacy=0&with_editor=false&ws_port={d.get(\"wsport\",8001)}')"
   ```
3. رقم الدرافت يتغيّر مع **كل** تشغيل لـ`preview`. فتح درافت قديم = تصميم قديم.
4. كروم يحجب `http://localhost:8000` داخل صفحة https. اسمح بالمحتوى غير الآمن
   لـ`s.salla.sa` من `chrome://settings/content/insecureContent`.
5. تعديلات `twilight.json` لا تظهر إلا بجلسة `preview` جديدة (لا بالتحديث الحي).

## ⚠️ مشكلة قائمة من طرف سلة (غير محلولة)

منذ 2026-08-12 توقّفت سلة عن استيعاب (ingest) نسخ الثيم. النتيجة:

- **لا تظهر أي إعدادات ثيم أضيفت بعد ذلك التاريخ** في لوحة التصميم
- `theme.settings.get()` تُرجع `null` لتلك المفاتيح
- لوحة "عناصر الصفحة" فارغة: «لا يحتوي هذا الثيم على عناصر قابلة للتخصيص»

**الأثر العملي:** أي إعداد جديد في `twilight.json` لن يعمل حاليًا. تجنّب بناء
ميزات تعتمد على إعداد جديد — استخدم قيمًا افتراضية في الكود بدلًا منها.

تم التحقق من سلامة كل ما يخصنا: `twilight.json` صالح ومطابق للثيم المرجعي،
صلاحيات GitHub سليمة، TwilightCI يعطي ✅، وأكثر من 45 تاق أُنشئت. رسالة الدعم
الجاهزة في سجل المحادثة السابقة.

## أعراف المشروع

- ملفات التخصيص منفصلة ببادئة `sawab-` تحت `src/assets/styles/04-components/`
  وتُستورَد في **آخر** `app.scss` لتفوز بالأولوية على أنماط الثيم الأساسي.
- الكلاسات المخصصة ببادئة `sawab-` أو `.sawab-card__*`.
- **RTL:** لا تستخدم الخصائص المنطقية (`margin-inline-start`) — PostCSS يحوّلها
  إلى خصائص فيزيائية متجاهلًا الاتجاه فتنعكس خطأً. استخدم نمط المشروع:
  ```scss
  [dir="rtl"] & { margin-right: auto; }
  [dir="ltr"] & { margin-left: auto; }
  ```
  أو شبكة CSS (العمود الأول = اليمين في RTL تلقائيًا).
- إعدادات الثيم تُمرَّر للجافاسكربت عبر `window.*` في `master.twig` (مثل
  `window.taxable_prices_enabled`) — لأن `salla.config.get()` لا يوفّر كل شيء.

## مزالق مكوّنات سلة (مهمة جدًا)

- **`{% component 'path' %}` للمكوّنات المسجّلة لدى سلة فقط.** استدعاء ملف
  مخصص به يُفشل الصفحة كلها (صفحة بيضاء). استخدم `{% include 'components.home.hero' %}`.
- **البطاقات في القوائم ليست من `product-card.js`.** كل القوائم (الرئيسية،
  التصنيفات، البحث) تستخدم `<salla-products-list>` وهو يرسم بطاقاته داخليًا.
  ملف `product-card.js` يخصّ `<custom-salla-product-card>` المستخدم في عناصر
  قليلة فقط. الكلاسات (`s-product-card-*`) مشتركة بين الاثنين، لذا **CSS يعمل
  على الجميع بينما JS لا**.
- **قراءة بيانات المنتج من البطاقة:** الخاصية العامة الوحيدة هي `product`.
  و`productData` حالة داخلية (`@State`) **لا تُعرَض على عنصر DOM**. والقائمة
  تمرّر كائنًا (خاصية) لا نصًا (سمة)، فـ`getAttribute` وحده لا يكفي.
- **حقل `brand` لا يعود في القوائم افتراضيًا.** يجب طلبه:
  `includes='["brand"]'` على `salla-products-list` و`salla-products-slider`.
  (ضبط `includes` يبدّل القائمة إلى مستمع تمرير يدوي — مسار مقصود من سلة.)
- **خيارات المنتج** تُرسم قائمة منسدلة افتراضيًا. لعرضها كبطاقات مرّر:
  ```html
  config='{"single-option":{"type":"button"},"multiple-option":{"type":"button"}}'
  ```
  وسلة تعرض السعر الإضافي لكل خيار تلقائيًا داخل `<p>`.
- **صفحة المنتج فيها كتلتا سعر.** الكلاسات `.total-price` و`.before-price`
  و`.price_is_on_sale` هي ما يحدّثه `product.js` عند تغيّر الخيار — أي كتلة
  سعر جديدة يجب أن تحملها وإلا بقيت ثابتة.
- `app.element()` يُرجع أول عنصر فقط (عدا قائمة `multiSelectors` في
  `app-helpers.js`). استخدم `querySelectorAll` عند وجود أكثر من مطابقة.
- **`salla-add-product-button` يوفّر "اشتر الآن"** عبر `quick-buy`، لكنه مشروط
  بإعداد `store.settings.buy_now` في لوحة المتجر. لا تبنِ زرًا منافسًا.

## ما تم إنجازه

**الهيدر** — `sawab-header.scss` + `header/header.twig`
شعار وهمبرغر يمينًا، أيقونات (بحث/لغة/حساب/سلة) يسارًا على الجوال، هيدر لاصق
بتأثير زجاجي عند التمرير، مبدّل اللغة/العملة، زر بحث يفتح مودال سلة، ونص
placeholder مخصص يُفرَض بـ`MutationObserver` (لأن سلة تجلبه من ترجماتها لا من
`src/locales`). شريط إعلاني بزر نسخ كود خصم اختياري.

**قسم الهيرو** — `components/home/hero.twig` + `sawab-hero.scss`
يُستدعى من `pages/index.twig` بـ`{% include %}`. يقرأ من `theme.settings.hero_*`
— **ولهذا يظهر فارغًا حاليًا** بسبب مشكلة سلة أعلاه.

**بطاقة المنتج** — `sawab-product-card.scss` + `partials/product-card-enhancer.js`
صورة مربعة، شارة `promotion_title`، اسم الماركة، الاسم بسطرين مع `...`، السعر مع
"شامل الضريبة"، وزر إضافة أحمر. المُحسِّن يحقن الماركة والضريبة في بطاقات سلة
بعد رسمها (تتبُّع مستقل لكل إضافة + `MutationObserver` + مرور متعدد للترطيب).

**صفحة المنتج** — `pages/product/single.twig` + `sawab-product-page.scss`
كتلة ماركة (شعار + اسم كرابط + "ضمان معتمد من الوكيل")، شارة "متوفر في المخزون"،
شارة نسبة الخصم، وخيارات كبطاقات.

## المتبقي

- مطابقة صفحة المنتج للتصميم المرجعي بصريًا (الصورة لدى المستخدم).
- تجاهل مؤقتًا: ميزة "يضيفها العملاء مع الخزان"، وكل ما أسفل صفحة المنتج.
- زر واتساب في الهيرو: أُبقي عليه بطلب المستخدم.
